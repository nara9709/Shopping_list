'use strict';

const addBtn = document.querySelector('.add__btn');
const items = document.querySelector('.items');
const deleteBtn = document.querySelector('.delete__btn');

function onAddBtn() {
  //1. get a value of text input
  //2. add list when input btn is clicked
  //3. reset
}
